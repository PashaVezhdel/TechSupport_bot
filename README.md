# 🛠 TechSupport Bot (TechSupportKlen)

**TechSupport Bot** — це автоматизована система для обробки звернень до технічної підтримки через Telegram. Проєкт розроблено як частину дипломної роботи для оптимізації комунікації між користувачами та IT-відділом.

Бот забезпечує повний цикл обробки заявки: від створення користувачем до закриття оператором підтримки, з використанням хмарної бази даних MongoDB Atlas.

---

## 🚀 Функціонал

### 👤 Для Користувача
* **Реєстрація:** Автоматична реєстрація при першому запуску (`/start`).
* **Створення заявки:** Покрокова форма (FSM) для подачі заявки:
    * Введення ПІБ.
    * Надсилання номера телефону (кнопкою або вручну з валідацією).
    * Опис проблеми.
    * Додавання фото або документу (або пропуск цього кроку).
    * Вибір пріоритету (Низький, Середній, Високий).
* **Історія:** Перегляд власних заявок зі статусами.
* **Скасування:** Можливість скасувати активну заявку до того, як її виконають.
* **Сповіщення:** Отримання повідомлень про зміну статусу заявки (Прийнята, Завершена, Відхилена з причиною).

### 👨‍💻 Для Технічної Підтримки
* **Рольовий доступ:** Доступ до панелі підтримки мають лише авторизовані адміністратори (перевірка через БД).
* **Активні заявки:** Перегляд загального списку нових заявок.
* **Обробка:**
    * **Прийняти:** Заявка закріплюється за конкретним сапортом.
    * **Завершити:** Успішне закриття заявки.
    * **Відхилити:** Обов'язкове введення причини відхилення (повідомляється користувачу).
* **Історія:** Перегляд архіву всіх закритих заявок.
* **Моніторинг:** Перевірка статусу підключення до бази даних.

---

## 🛠 Технології

* **Python 3.10+**
* **aiogram 3.x** — асинхронний фреймворк для Telegram API.
* **MongoDB (Atlas)** — хмарна NoSQL база даних.
* **pymongo** — драйвер для роботи з БД.
* **python-dotenv** — керування змінними оточення (безпека даних).

---

## 📂 Структура проєкту

```text
TechSupport_bot/
├── app/
│   ├── db/            # Підключення до БД та функції (CRUD)
│   ├── filters/       # Фільтри для розмежування прав доступу (User/Support)
│   ├── fsm/           # Машини станів (FSM)
│   ├── handlers/      # Логіка обробки команд
│   └── keyboards/     # Клавіатури та інлайн-кнопки
├── config.py          # Конфігурація бота
├── main.py            # Точка входу (запуск бота)
├── requirements.txt   # Список залежностей
├── .env               # Секретні змінні (Токен, БД - не додається в репозиторій)
└── README.md          # Документація