# 🛠 TechSupport Bot (TechSupportKlen)

**TechSupport Bot** — це автоматизована система для обробки звернень до технічної підтримки через Telegram. Проєкт розроблено як частину дипломної роботи для оптимізації комунікації між користувачами та IT-відділом.

Система підтримує роботу як у хмарі, так і на локальному сервері (On-Premise) завдяки повній контейнеризації через **Docker**.

---

## 🚀 Функціонал

### 👤 Для Користувача
* **Реєстрація:** Автоматична реєстрація та збереження профілю при першому запуску (`/start`).
* **Створення заявки:** Інтерактивна форма (FSM) для подачі заявки:
    * Введення ПІБ.
    * Надсилання номера телефону (кнопкою або вручну з валідацією формату).
    * Опис суті звернення.
    * Додавання медіа-файлів (фото або документ) або пропуск кроку.
    * Вибір пріоритету (Низький, Середній, Високий).
* **🔔 Виклик у серверну:** Кнопка термінового виклику адміністратора з автоматичним сповіщенням персоналу.
* **🛡 Анти-спам:** Система захисту від флуду (обмеження на виклик — не частіше 1 разу на 5 хвилин).
* **Історія:** Перегляд власних заявок з актуальними статусами.
* **Скасування:** Можливість самостійно скасувати активну заявку.
* **Сповіщення:** Автоматичні повідомлення про зміну статусу заявки.

### 👨‍💻 Для Технічної Підтримки (Адміністратора)
* **Рольовий доступ:** Доступ лише для авторизованих співробітників (перевірка в БД).
* **Активні заявки:** Перегляд загальної черги нових та прийнятих заявок.
* **Обробка заявок:**
    * **Перегляд медіа:** Фото та документи відображаються прямо в картці заявки.
    * **Прийняти:** Закріплення заявки за собою (користувач бачить виконавця).
    * **Завершити:** Закриття виконаної заявки одним кліком.
    * **Відхилити:** Обов'язкове введення причини відмови.
* **📨 Масова розсилка:** Підтримка тексту та медіа з попереднім переглядом перед відправкою.
* **Реакція на виклики:** Швидка відповідь на виклик у серверну ("👍" / "👎").

### 👑 Для Супер-Адміністратора (Нове)
* **Керування персоналом:** Динамічна зміна нижньої панелі кнопок (Reply Keyboard) для керування командою без захаращення чату.
* **Гнучке налаштування ролей:**
    * **Додавання адмінів:** Призначення нових співробітників за Telegram ID через інтерфейс бота.
    * **Звільнення адмінів:** Зручний список з можливістю видалення прав (із захистом від самовидалення).
* **Стабільність інтерфейсу:** Примусове оновлення меню для Супер-Адміністратора, що запобігає зникненню кнопок керування.
* **Чистий чат:** Усунено дублювання службових повідомлень завдяки переходу на Reply-навігацію.

---

## 🛠 Технології

* **Python 3.11** — основна мова розробки.
* **aiogram 3.x** — асинхронний фреймворк для Telegram Bot API.
* **MongoDB (Atlas)** — NoSQL база даних.
* **pymongo** — драйвер для роботи з MongoDB.
* **Docker & Docker Compose** — контейнеризація застосунку.
* **python-dotenv** — керування змінними оточення.

## 📂 Структура проєкту

```text
TechSupport_bot/
├── app/
│   ├── db/            # Підключення до БД (MongoDB) та CRUD-функції
│   ├── filters/       # Кастомні фільтри прав доступу
│   ├── fsm/           # Машини станів (Ticket, Broadcast, AdminManage)
│   ├── handlers/      # Логіка повідомлень (User, Support, Errors)
│   └── keyboards/     # Клавіатури (Reply та Inline)
├── config.py          # Конфігурація токенів та URI
├── main.py            # Точка входу та реєстрація роутерів
├── requirements.txt   # Залежності проєкту
├── Dockerfile         # Інструкції збірки образу
├── docker-compose.yml # Налаштування контейнерів
├── Procfile           # Команда запуску для хмарних сервісів
└── .env               # Змінні оточення (BOT_TOKEN, MONGO_URI)
