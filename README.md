# 🛠 TechSupport Bot (TechSupportKlen)

**TechSupport Bot** — це автоматизована система для обробки звернень до технічної підтримки через Telegram. Проєкт розроблено як частину дипломної роботи для оптимізації комунікації між користувачами та IT-відділом.

Система підтримує роботу як у хмарі, так і на локальному сервері (On-Premise) завдяки повній контейнеризації через **Docker**.

---

## 🚀 Функціонал

### 👤 Для Користувача
* **Реєстрація:** Автоматична реєстрація та збереження профілю при першому запуску (`/start`).
* **Створення заявки:** Інтерактивна форма (FSM) для подачі заявки:
    * Введення ПІБ.
    * Надсилання номера телефону (кнопкою або вручну з валідацією формату).
    * Опис суті звернення.
    * Додавання медіа-файлів (фото або документ) або пропуск цього кроку.
    * Вибір пріоритету (Низький, Середній, Високий).
* **🔔 Виклик у серверну:** Кнопка термінового виклику адміністратора з автоматичним сповіщенням персоналу.
* **🛡 Анти-спам:** Система захисту від флуду (обмеження на виклик у серверну — не частіше 1 разу на 5 хвилин).
* **Історія:** Перегляд списку власних заявок з актуальними статусами.
* **Скасування:** Можливість самостійно скасувати активну заявку до того, як її візьмуть в роботу.
* **Сповіщення:** Отримання автоматичних повідомлень про зміну статусу заявки (Прийнята, Завершена, Відхилена з причиною).

### 👨‍💻 Для Технічної Підтримки (Адміністратора)
* **Рольовий доступ:** Доступ до панелі управління мають лише авторизовані співробітники (перевірка ID через базу даних).
* **Активні заявки:** Перегляд загальної черги нових та прийнятих заявок від усіх користувачів.
* **Обробка заявок:**
    * **Перегляд медіа:** Фото та документи відображаються прямо в картці заявки.
    * **Прийняти:** Закріплення заявки за собою (користувач бачить, хто прийняв).
    * **Завершити:** Закриття виконаної заявки одним кліком.
    * **Відхилити:** Обов'язкове введення причини відмови (надсилається користувачу).
* **📨 Масова розсилка:**
    * Створення повідомлень для всіх користувачів бота.
    * Підтримка тексту, фото, відео та документів.
    * Попередній перегляд повідомлення перед відправкою.
    * Збереження історії розсилок та статистики доставки в БД.
* **Реакція на виклики:** Отримання сповіщень про виклик у серверну з кнопками швидкої відповіді ("👍" / "👎").
* **Архів та Моніторинг:**
    * Перегляд історії всіх закритих/відхилених заявок.
    * Перевірка статусу з'єднання з базою даних.

---

## 🛠 Технології

* **Python 3.11** — основна мова розробки.
* **aiogram 3.x** — сучасний асинхронний фреймворк для Telegram Bot API.
* **MongoDB (Atlas)** — хмарна NoSQL база даних.
* **pymongo** — офіційний драйвер для роботи з MongoDB.
* **Docker & Docker Compose** — контейнеризація застосунку для швидкого розгортання та ізоляції.
* **python-dotenv** — керування змінними оточення.
* **logging** — система логування подій.

## 📂 Структура проєкту

```text
TechSupport_bot/
├── app/
│   ├── db/            # Підключення до БД, колекції та CRUD-функції
│   ├── filters/       # Кастомні фільтри (Admin/User)
│   ├── fsm/           # Машини станів (FSM)
│   ├── handlers/      # Логіка обробки повідомлень
│   └── keyboards/     # Клавіатури (Reply/Inline)
├── config.py          # Конфігурація
├── main.py            # Точка входу
├── requirements.txt   # Залежності
├── Dockerfile         # Налаштування образу Docker
├── docker-compose.yml # Налаштування контейнера
├── .dockerignore      # Виключення для Docker
└── .env               # Змінні оточення
